
#include <iostream>
#include <cmath>
using namespace std;
int main()
{

	// Homw Work Triangle Area // 
	/*
	float a, d,Area;
	cout << "Enter a: ";
	cin >> a;
	cout << "Enter d: ";
	cin >> d;
	Area = a * sqrt(pow(d, 2) - pow(a, 2));
	cout << "Reactangel Area is: " << Area << endl;


	// Hom Work Circle Area inscribed in a Square // 
	const float pt = 3.14;
	float a, Area;
	cout << "Enter a: ";
	cin >> a;
	//Area = pt * pow(a, 2) / 4;
	Area = pt * pow((a/2),2);
	cout << "Cirle Area is: " << Area;


	// Hom Work Circle Area //
	const float pt = 3.14;
	float r, Area;
	cout << "Enter r: ";
	cin >> r;
	Area = pt * pow(r, 2);
	cout << "Circle Area is: " << r << endl;


	// Hom Work Circle Area Throug Diameter //
	const float pt = 3.14;
	float d, Area;
	cout << "Enter d: ";
	cin >> d;
	Area = pt * pow(d, 2) / 4;
	cout << "Circle Area is: " << Area << endl;


	// Hom Work Circle Area Along the Circumference //

	const float pt = 3.14;
	float l, Area;
	cout << "Enter l: ";
	cin >> l;
	Area = pow(l,2) /(4*pt);
	cout << "Circle Area is: " << Area << endl;


	// Hom Work Circle Area Inscribed in Isosceles Triangle //

	const float pt = 3.14;
	float a,b, Area;
	cout << "Enter a: ";
	cin >> a;
	cout << "Enter b: ";
	cin >> b;
	Area =  pt * (pow(b,2) / 4 ) * ((2 * a - b) / (2 * a + b));
	cout << "Circle Area is: " << Area << endl;


	// Home Work Circle Area Circle Described Around an Arbitrary Triangle //

	const float pt = 3.14;
	float a, b,c,p, Area;
	cout << "Enter a: ";
	cin >> a;
	cout << "Enter b: ";
	cin >> b;
	cout << "Enter c: ";
	cin >> c;
	p = (a + b + c) / 2;
	Area = pt * pow( ((a * b * c) / (4 * sqrt( p * (p - a) * (p - b) * (p - c ) ))), 2);
	cout << "Circle Area is: " << Area << endl;


	// Home Work Power of 2,3,4 //

	int num, num1, num2, num3;
	cout << "Enter Number :";
	cin >> num;
	num1 = pow(num, 2);
	num2 = pow(num, 3);
	num3 = pow(num, 4);
	cout << num1 << endl << num2 << endl << num3 << endl;

	// Home Work Power of M //

	int num, M, result;
	cout << "Enter Number: ";
	cin >> num;
	cout << "Enter Power: ";
	cin >> M;
	result = pow(num, M);
	cout << "Result is: " << result << endl;


	// Home Work Task Duration in Seconds //

float duration, seconds, days, hours, minutes;
cout << "days: ";
cin >> days;
cout << "hours: ";
cin >> hours;
cout << "minutes: ";
cin >> minutes;
cout << "seconds: ";
cin >> seconds;

duration = days * 24 * pow(60, 2) + hours * pow(60, 2) + minutes * 60 + seconds;
cout << "duration in seconds is: " << round(duration) << " seconds" << endl;


// Home Work Task Secpmds tp Days Hours Minutes //
float day = 24 * pow(60, 2), hour = pow(60, 2), minute = 60;
float seconds ,days,hours,minutes, remainder;
cout << "Number Of Seconds: ";
cin >> seconds;
days = floor(seconds / day);
remainder = seconds % days;
hours = floor(remainder / hour);
remainder = remainder % hours;
minutes = floor(remainder / minute);
remainder = remainder % minutes;
seconds = remainder;
cout << days << ":" << hours << ":" << minutes << ":" << seconds << endl;

	*/



	
	



}
